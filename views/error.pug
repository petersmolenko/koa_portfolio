extends ./_template

block variables
  - var title = 'Ошибка'

block content
  header.header
    .container.header__container
      .header__top
        .header__text.title-name Архипов
          span.title-name__doter .
        .header__social
          ul.social
            each item in social
              li.social__item
                a.social__link(href="http://")
                  i(class=`fab ${item}`)
      .header__content(style="margin-top: 20%")
        h1.header__title #{error}
        a(href="/index").header__link Вернуться домой

